# Regenerate after installing the Sage theme:
#   ./vendor/bin/phpstan analyse --generate-baseline=phpstan-baseline.neon
# This snapshots existing theme errors so CI stays green while still failing
# on newly introduced ones.
parameters:
	ignoreErrors: []
